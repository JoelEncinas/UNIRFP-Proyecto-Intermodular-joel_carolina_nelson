spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/bike_share_db}
spring.datasource.username=${DB_USERNAME:bikeshare}
spring.datasource.password=${DB_PASSWORD:bikeshare}
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}

security.jwt.secret=${JWT_SECRET:bikesharebikesharebikesharebikesharebikeshare123456}
security.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
security.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}

payments.stripe.secret-key=${STRIPE_SECRET_KEY:}
payments.stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET:}
payments.stripe.success-url=${STRIPE_SUCCESS_URL:http://localhost:4200/app/map?payment=success}
payments.stripe.cancel-url=${STRIPE_CANCEL_URL:http://localhost:4200/app/map?payment=cancel}
payments.stripe.currency=${STRIPE_CURRENCY:eur}
payments.rental.unlock-fee=${RENTAL_UNLOCK_FEE:1}